apiVersion : v1
kind: PersistentVolume
metadata:
  name: jobee-pv
spec:
  accessModes :
  - ReadWriteOnce
  capacity:
    storage: 200M
  hostPath:
    path: /tmp/pvexmaple
  persistentVolumeReclaimPolicy: Recycle
  storageClassName: manual

